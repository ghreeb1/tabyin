# 📘 شرح الكود والتقنيات المستخدمة في مشروع "تبيّن" (Tabayyun) v2

هذا الملف يقدم شرحاً شاملاً للتقنيات المستخدمة وهيكلية الكود في النسخة الثانية من مشروع "تبيّن"، وهو مساعد قانوني ذكي مصمم لخدمة المواطنين في المملكة العربية السعودية.

---

## 🛠️ التقنيات المستخدمة (Technology Stack)

يعتمد المشروغ على مجموعة من التقنيات الحديثة لبناء تطبيق ويب تفاعلي وذكي:

### 1. الـ Backend (الخلفية البرمجية)
- **Python**: لغة البرمجة الأساسية للمشروع.
- **Flask**: إطار عمل ويب خفيف وسريع لبناء السيرفر والتحكم في المسارات (Routes) والربط بين الواجهة وقواعد البيانات.
- **Flask-SQLAlchemy**: مكتبة للتعامل مع قاعدة البيانات بطريقة كائنية (ORM)، مما يسهل تخزين واسترجاع بيانات المستخدمين.
- **Flask-Login**: إدارة جلسات المستخدمين (تسجيل الدخول، الخروج، وحماية الصفحات).

### 2. الذكاء الاصطناعي (AI & LLM)
- **Google Gemini API** (`google-generativeai`): المحرك الذكي للإجابة على استفسارات المستخدمين. تم توجيهه (Prompt Engineering) ليعمل كمساعد قانوني سعودي، مع تعليمات صارمة لتقديم إجابات دقيقة وموجزة.
  - **النموذج المستخدم**: `gemini-flash-latest` (أو ما يماثله من عائلة Gemini) للسرعة والكفاءة.

### 3. الخدمات الصوتية (Voice Services)
المشروع يدعم التحدث مع المساعد والاستماع للإجابات باستخدام تقنيات متقدمة تعمل محلياً (Offline) أو عبر مكتبات قوية:
- **Speech-to-Text (تحويل الصوت إلى نص)**:
  - **Faster-Whisper**: نسخة محسنة وسريعة من نموذج Whisper الخاص بـ OpenAI. تعمل محلياً لتحويل صوت المستخدم إلى نص بدقة عالية، وتدعم اللغة العربية بلهجات مختلفة.
- **Text-to-Speech (تحويل النص إلى صوت)**:
  - **gTTS (Google Text-to-Speech)**: لتحويل نص الإجابة إلى ملف صوتي.
  - **FFmpeg**: أداة قوية لمعالجة ملفات الصوت، تُستخدم هنا لتسريع الصوت الناتج (بمعدل 1.3x) ليكون الرد أكثر طبيعية وسلاسة، وكذلك لتحويل صيغ الملفات الصوتية لضمان التوافق.

### 4. قاعدة البيانات (Database)
- **SQLite**: قاعدة بيانات خفيفة ومدمجة، تُستخدم حالياً لتخزين بيانات المستخدمين (الاسم، البريد الإلكتروني، كلمة المرور المشفرة).

### 5. الواجهة الأمامية (Frontend)
- **HTML5 & CSS3**: لبناء هيكل وتصميم الصفحات (قوالب `templates`).
- **JavaScript**: لإدارة التفاعلات في صفحة المحادثة (إرسال الرسائل، التسجيل الصوتي، تشغيل الردود الصوتية) دون إعادة تحميل الصفحة (AJAX/Fetch API).

---

## 📂 هيكلية الكود (Code Structure)

شرح لأهم الملفات والمجلدات في المشروع:

### `app.py`
هو الملف الرئيسي لتشغيل التطبيق، ويحتوي على:
- **إعدادات السيرفر**: تهيئة Flask وقاعدة البيانات والمفاتيح السرية.
- **المسارات (Routes)**: الروابط المختلفة للموقع (الرئيسية `/home`، الدردشة `/chat`، تسجيل الدخول `/login`، إلخ).
- **منطق الذكاء الاصطناعي**: دالة `ask_gemini` التي ترسل السؤال إلى Google Gemini مع "Sytem Prompt" يحدد شخصية المساعد القانوني.
- **نقاط الاتصال (APIs)**: 
  - `/voice-to-text`: تستقبل ملف الصوت، تحوله لنص، تسأل Gemini، ثم تعيد الرد كنص وصوت.
  - `/text-to-speech`: تحويل أي نص إلى صوت عند الطلب.

### `voice_service.py`
ملف منفصل لتنظيم الخدمات الصوتية، يحتوي على دوال مثل:
- `transcribe_audio`: تستقبل مسار الملف الصوتي وتستخدم Faster-Whisper لاستخراج النص.
- `generate_speech`: تحول النص إلى صوت باستخدام gTTS، ثم تقوم بتسريعه.
- `speed_up_audio` & `convert_to_wav`: دوال مساعدة تستخدم FFmpeg لمعالجة الملفات الصوتية.

### `database.py`
يحتوي على تعريف جداول قاعدة البيانات (Models):
- **User**: جدول المستخدمين (المعرف، الاسم، الإيميل، وكلمة المرور المشفرة).

### `templates/`
مجلد يحتوي على ملفات HTML الخاصة بصفحات الموقع (مثل `chat.html`, `login.html`, `home.html`).

### `requirements.txt`
قائمة بالمكتبات اللازمة لتشغيل المشروع (مثل `flask`, `google-generativeai`, `faster-whisper`, إلخ).

---

## 🚀 كيف يعمل النظام (Workflow)

1. **الدردشة النصية**:
   - يكتب المستخدم السؤال -> يُرسل للـ Backend -> يتم تمريره لـ Gemini -> يعود الجواب ويُعرض للمستخدم.

2. **الدردشة الصوتية**:
   - يسجل المستخدم صوته -> يُرسل الملف للسيرفر.
   - يقوم `voice_service` بتحويل الصوت لنص (Whisper).
   - يُرسل النص لـ Gemini للحصول على الإجابة.
   - يُحول نص الإجابة إلى صوت (gTTS + FFmpeg).
   - يعود للسيرفر ملف صوتي والرد النصي ليتم تشغيله وعرضه أمام المستخدم.

---

## 💡 ملاحظات للتطوير (Future Improvements)
- يمكن ترقية قاعدة البيانات إلى PostgreSQL أو MySQL في حال التوسع.
- يمكن تحسين واجهة المستخدم باستخدام إطارات عمل مثل React أو Vue.js لمزيد من التفاعلية.
- إضافة ميزة "حفظ المحادثات" في قاعدة البيانات ليعود لها المستخدم لاحقاً.
